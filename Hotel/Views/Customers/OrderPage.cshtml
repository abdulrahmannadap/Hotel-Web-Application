@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Customer
@using(Html.BeginForm("Save","Customers",FormMethod.Post))
{
    <form class="col-form-label-sm form-groub" >
        <div >

            @Html.LabelFor(custm => custm.CustomerName)
            @Html.TextBoxFor(custm => custm.CustomerName,new {@class="form-conteol"})           
        </div>
        
        <div>
            @Html.LabelFor(custm => custm.OrderList)
            @Html.DropDownListFor(custm => custm.OrderListId,
        new SelectList(ViewBag.OrderListData ,"Id","DisheName"),"---- SELECT ORDER DISHES ----",
                         new {@class="form-control"}
                                 )
        </div>
        @Html.HiddenFor(cutm => cutm.Id)
        <div>
            <input type="submit" value="SUBMIT ORDER" class="btn btn-info"/>
        </div>
    </form>
}